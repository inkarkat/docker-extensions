#!/bin/bash

set -o pipefail
readonly auth=$(jq -r '.auths."docker.hos.hpecorp.net".auth' ~/.docker/config.json | base64 --decode)
curl --silent -L -u "$auth" https://api.docker.hos.hpecorp.net/v2/${1:?}/tags/list | \
    jq -r '.tags | join("\n")'
