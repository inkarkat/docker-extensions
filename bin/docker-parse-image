#!/bin/bash

if [ $# -ge 8 ] && [[ "$1" =~ ^.*/[^:]+$ ]] && [[ "$3" =~ ^[0-9a-f]{12}$ ]]; then
    printf '%s:%s\n' "$1" "$2"
elif [ $# -ge 7 ] && [[ "$1" =~ ^.*/[^:]+:[^:]+$ ]] && [[ "$2" =~ ^[0-9a-f]{12}$ ]]; then
    printf '%s\n' "$1"
else
    printf '%s\n' "$*"
fi
